doctype html
html

	head
		include ./shared/head
		link(rel="stylesheet", type="text/css", href="/src/css/tooltip.css")

	body

		include ./shared/header

		main.container
			div
				a.mk-tt(href="#", data-label="This is a mo fuckin test")
					| Tooltip

			div(style="position:relative")
				button.mk-tt
					| Tooltip
				div.mk-tt-modal
					| This is a tooltip baby

			div
				table
					tbody
						tr
							td
								button.mk-tt
									| Tooltip
								div.mk-tt-modal
									| This is a tooltip in a table
							td
								button.mk-tt(aria-describedby="test")
									| Tooltip
			div#test.mk-tt-modal
				| This is in the parent




		include ./shared/scripts
		script(type="text/javascript", src="/src/js/standard/tooltip.js")
		script.
			
			mkNasty.transitions(true);

			var tooltip = new mkNasty.Tooltip('.container');